---
import worked from "@collections/en/worked.json";
import Layout from "@layouts/main.astro";
import WorkedBase from '@components/page/worked.astro'
import { getLangFromUrl, useTranslations } from '@i18n/utils';
const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const prefixLocale = false;
---

<Layout language="en" title={t('worked.title')} prefixLocale={prefixLocale}>
  <WorkedBase worked={worked}/>
</Layout>
