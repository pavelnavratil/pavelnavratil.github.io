---
import projects from "@collections/en/projects.json";
import ProjectsBase from '@components/page/projects.astro'
const prefixLocale = false;
---
<ProjectsBase projects={projects} prefixLocale={prefixLocale}/>
